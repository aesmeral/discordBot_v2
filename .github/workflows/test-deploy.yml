name: remote ssh command
on: [push]
jobs:

  build:
    name: Build
    runs-on: self-hosted
    steps:
    - name: executing remote ssh commands using password
      uses: actions/checkout@v2
      run: ssh -i ${{secrets.PKEY}} anthony@192.168.1.31 && ll -t
