name: remote ssh command
on: [push]
jobs:

  build:
    name: Build
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v2
      # testing
    - name: executing remote ssh commands using password
      run: ssh -i ${{secrets.PKEY}} anthony@192.168.1.31
